using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PowerLeveling : MonoBehaviour {

	//destroy the map and everything on it, camera too
	//reinstanciate map, reset the values to start values, up the difficulty
	void SetUpLevel(int level){

	}
}
